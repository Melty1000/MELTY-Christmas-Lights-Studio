#!/usr/bin/env sh

msg_file="$1"

model_line="$(grep -Ei '^AI-Model:[[:space:]]*[^[:space:]].*$' "$msg_file" || true)"
machine_line="$(grep -Ei '^Commit-Machine:[[:space:]]*[^[:space:]].*$' "$msg_file" || true)"

if [ -z "$model_line" ] || [ -z "$machine_line" ]; then
  echo ""
  echo "Commit metadata is required."
  echo ""
  echo "Add these trailers to the commit message:"
  echo ""
  echo "AI-Model: GPT-5 Codex"
  echo "Commit-Machine: desktop"
  exit 1
fi
